<%= form_with(model: todo, local: true) do |form| %>
  <% if todo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(todo.errors.count, "error") %> prohibited this todo from being saved:</h2>

      <ul>
      <% todo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :item %>
    <%= form.text_field :item, id: :todo_item %>
  </div>

  <div class="field">
    <%= form.label :done %>
    <%= form.text_field :done, id: :todo_done %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
